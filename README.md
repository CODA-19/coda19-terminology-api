# CODA-19 terminology API
Terminology API for CODA-19.

## Requirements

NodeJS and NPM, and install dependencies:

```bash
npm install
npm install lodash --save
node --version > v11
```

## Export JSON

```bash
node index.js
```
